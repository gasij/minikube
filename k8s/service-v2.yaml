apiVersion: v1
kind: Service
metadata:
  name: myapp-v2-service
  labels:
    app: myapp
    version: v2
spec:
  type: NodePort
  selector:
    app: myapp
    version: v2
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30081
