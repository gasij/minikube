apiVersion: v1
kind: Service
metadata:
  name: myapp-v1-service
  labels:
    app: myapp
    version: v1
spec:
  type: NodePort
  selector:
    app: myapp
    version: v1
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
